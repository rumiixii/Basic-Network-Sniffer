# Basic-Network-Sniffer
This project entails creating a simple network sniffer that prints out the source and destination IP addresses of the packet, the protocol being used and the payload. The language used is Python
